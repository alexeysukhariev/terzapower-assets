# terzapower-assets

Static assets (images, 3D textures, three.js, app.js) for the TerzaPower landing.
Served to the Tilda-hosted page via jsDelivr CDN. Versioned by git tag.
No secrets here — the lead webhook + token live in Vercel env.
